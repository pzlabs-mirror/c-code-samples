#ifndef SEARCH_COMMON_H
#define SEARCH_COMMON_H

#include <stddef.h>

/// @brief A special size_t value used to represent a non-existent array index
/// of an element that cannot be found by a search algorithm
#define SEARCH_NPOS ((size_t)-1)

#endif // include guard
